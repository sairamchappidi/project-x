// import { fromJS } from 'immutable';
// import { makeSelectSampleContainerDomain } from '../selectors';

// const selector = makeSelectSampleContainerDomain();

describe('makeSelectSampleContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
